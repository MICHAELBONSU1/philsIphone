# TODO - Almighty Panel UI Enhancement

## Steps
1. Improve Almighty panel visual hierarchy (add controls row / section anchors if applicable).
2. Enhance tables/cards styling for readability (hover, spacing, responsive actions).
3. Add empty-state blocks for users/items when lists are empty.
4. Make panel dark-mode compatible by removing hardcoded light body background and adding dark overrides.
5. Quick manual verification: open `/almighty_panel` in light + dark mode.

